package libraryBooks;

class PhysicalBook extends Book {
    public PhysicalBook(String title, String author, String ISBN, int publicationYear, String genre) {
        super(title, author, ISBN, publicationYear, genre);
    }
}
